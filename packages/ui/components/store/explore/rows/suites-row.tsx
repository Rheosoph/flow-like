"use client";

import { SuitesRail } from "../../suites";

/** The `suites` rail renders the existing Suites & Platforms rail; it hides itself when the hub has none. */
export function SuitesRow() {
	return <SuitesRail />;
}
