---
title: "IMAP Create Mailbox (If Missing) Node"
description: "IMAP Create Mailbox (If Missing) Flow-Like node: Creates a mailbox if it doesn't exist; no-op if it already exists."
sidebar:
  label: "IMAP Create Mailbox (If Missing)"
head:
  - tag: meta
    attrs:
      name: keywords
      content: "IMAP Create Mailbox (If Missing), IMAP Create Mailbox (If Missing) node, Flow-Like node, Flow-Like node catalog, workflow automation node, Email/IMAP, web, mail_imap_create_mailbox, Email, IMAP, pins, schemas, security rating"
  - tag: meta
    attrs:
      property: article:section
      content: "Email/IMAP"
  - tag: script
    attrs:
      type: application/ld+json
    content: "{\"@context\":\"https://schema.org\",\"@type\":\"TechArticle\",\"headline\":\"IMAP Create Mailbox (If Missing) Node\",\"name\":\"IMAP Create Mailbox (If Missing)\",\"description\":\"IMAP Create Mailbox (If Missing) Flow-Like node: Creates a mailbox if it doesn't exist; no-op if it already exists.\",\"url\":\"https://docs.flow-like.com/nodes/email/imap/mail-imap-create-mailbox/\",\"isPartOf\":{\"@type\":\"CollectionPage\",\"name\":\"Flow-Like Node Catalog\",\"url\":\"https://docs.flow-like.com/nodes/overview/\"},\"about\":\"Email/IMAP\",\"keywords\":\"IMAP Create Mailbox (If Missing), IMAP Create Mailbox (If Missing) node, Flow-Like node, Flow-Like node catalog, workflow automation node, Email/IMAP, web, mail_imap_create_mailbox, Email, IMAP, pins, schemas, security rating\",\"programmingLanguage\":\"Rust\",\"mainEntity\":{\"@type\":\"SoftwareSourceCode\",\"name\":\"IMAP Create Mailbox (If Missing)\",\"description\":\"Creates a mailbox if it doesn't exist; no-op if it already exists\",\"codeSampleType\":\"Flow-Like node\",\"runtimePlatform\":\"Flow-Like\",\"programmingLanguage\":\"Rust\"}}"
---

import { NodeReference } from "../../../../../components/node-docs/NodeReference";
import { nodesBySlug } from "../../../../../generated/catalog-nodes";

<NodeReference node={nodesBySlug["nodes/email/imap/mail-imap-create-mailbox"]} />
